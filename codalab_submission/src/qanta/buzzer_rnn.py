import rnn

class RNNBuzzer:
    # TODO: Emily - Insert all the code that belongs here and debug
    def __init__(self):
        self.rnn_model = rnn.RNNBuzzer()


    def train(self, training_data, device):
        '''
        train the rnn buzzer using the rnn model and additional methods
        :param training_data:
        :param device:
        :return:
        '''

    def guess(self, guesser_model, question):
        '''
        Guess on a question text based on a guesser model with additional methods
        :param question: text input of type string
        :return: True or False
        '''

    def save(self, path):
        '''
        Save the RNN Buzzer
        :param path: path to save it as
        '''

    def load(self, path):
        '''
        Load the RNN Buzzer
        :param path: path to save it as
        '''

